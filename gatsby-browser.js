// import React from "react";
import "@fontsource/fira-mono" // Defaults to weight 400 with all styles included.
// import "./src/normalize.css"; // normalize CSS across browsers
// import "./src/style.css"; // custom CSS styles

// export const wrapPageElement = ({ element, props }) => {
//   return <Layout {...props}>{element}</Layout>;
// };
